fastapi
uvicorn[standard]
httpx
pydantic
python-dotenv

# LLM Fallback Service (ported from LLM Fallback 3)
litellm>=1.0.0

# Optional: semantic caching
# sentence-transformers>=2.2.0
# numpy>=1.24.0

# Optional: Redis cache backend
# redis>=5.0.0

# Optional: Momento serverless cache backend
# momento>=1.5.0
structlog>=23.1.0,<25.0.0

# PII detection & anonymization (content-based, complements field-name scrubbing)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.4.0,<4.0.0
# spaCy small English model — required by presidio-analyzer
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
