This directory is reserved for experiment artifacts:

- MLflow runs and metadata (if stored locally).
- Evaluation reports for RAG / generation quality.
- Comparison notebooks and analysis outputs.

In many deployments, long‑term experiment storage is moved to
remote object storage; this folder then holds only pointers or
lightweight summaries checked into Git.

