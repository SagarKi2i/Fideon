# Runtime-only dependencies for the API Docker image.
#
# Full development stack: see requirements.txt (includes GPU training wheels).
# This file omits pytest and packages only used by fine_tuning/ offline jobs
# (datasets, peft, trl, accelerate, bitsandbytes, torchvision) so the image
# stays small. PyTorch CPU wheels are installed in the Dockerfile before this file.

# --- Core API ---
fastapi
uvicorn[standard]
slowapi>=0.1.9
httpx
pydantic
python-dotenv
python-multipart>=0.0.9
PyJWT>=2.0.0
structlog>=23.1.0,<25.0.0
pyyaml>=6.0

# --- RunPod orchestration / remote control ---
paramiko>=3.4.0,<4.0.0

# --- LLM fallback + cache backends ---
litellm>=1.82.0,<1.83
numpy>=1.24.0
sentence-transformers>=2.2.0
redis>=5.0.0
momento>=1.5.0

# --- PII detection ---
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.4.0,<4.0.0

# --- Document/OCR processing ---
PyPDF2
pypdf
PyMuPDF
python-docx
openpyxl
Pillow
pytesseract
pdfplumber
rapidfuzz

# --- Vector/RAG ---
psycopg[binary]>=3.1.0
pgvector

# --- Inference (CPU); torch is preinstalled in Dockerfile ---
transformers>=4.36.0
huggingface-hub
peft>=0.7.0
