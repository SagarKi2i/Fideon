# Runtime-only dependencies for the API Docker image.
#
# Full development stack: see requirements.txt (includes GPU training wheels,
# litellm, spacy, sentence-transformers, redis, momento, etc.).
# Heavy ML/NLP packages are intentionally excluded here — they are lazy-imported
# and gracefully degrade when absent. Keeping this file lean cuts the image from
# ~6 GB down to ~1 GB and eliminates the pip-cache disk-exhaustion CI failure.

# --- Core API ---
fastapi
uvicorn[standard]
slowapi>=0.1.9
httpx
pydantic
python-dotenv
python-multipart>=0.0.9
PyJWT>=2.0.0
structlog>=23.1.0,<25.0.0
pyyaml>=6.0

# --- Webhooks (HMAC signed delivery) ---
cryptography>=42.0.0

# --- RunPod orchestration / remote control ---
paramiko>=3.4.0,<4.0.0

# --- Document/OCR processing ---
PyPDF2
pypdf
PyMuPDF
python-docx
openpyxl
Pillow
pytesseract
pdfplumber
rapidfuzz

# --- Vector/RAG ---
psycopg[binary]>=3.1.0
pgvector

# --- SeaweedFS / S3-compatible object store (GGUF adapter delivery) ---
boto3>=1.34.0
botocore>=1.34.0
