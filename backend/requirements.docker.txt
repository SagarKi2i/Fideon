# Runtime-only dependencies for the API Docker image.
#
# Full development stack: see requirements.txt (includes GPU training wheels).
# This file omits pytest and packages only used by fine_tuning/ offline jobs
# (datasets, peft, trl, accelerate, bitsandbytes, torchvision) so the image
# stays small. PyTorch CPU wheels are installed in the Dockerfile before this file.

# --- Core API ---
fastapi
uvicorn[standard]
slowapi>=0.1.9
httpx
pydantic
python-dotenv
python-multipart>=0.0.9
PyJWT>=2.0.0
structlog>=23.1.0,<25.0.0
pyyaml>=6.0

# --- Webhooks (HMAC signed delivery) ---
cryptography>=42.0.0

# --- RunPod orchestration / remote control ---
paramiko>=3.4.0,<4.0.0

# --- LLM fallback + cache backends ---
litellm>=1.82.0,<1.83
numpy>=1.24.0
sentence-transformers>=2.2.0
redis>=5.0.0
momento>=1.5.0

# --- PII detection ---
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.4.0,<4.0.0

# --- Document/OCR processing ---
PyPDF2
pypdf
PyMuPDF
python-docx
openpyxl
Pillow
pytesseract
pdfplumber
rapidfuzz
# NOTE: Paddle OCR deps are optional and moved to requirements.paddle.txt
# to avoid frequent install failures on slim images / Python 3.11.

# --- Vector/RAG ---
psycopg[binary]>=3.1.0
pgvector

# --- Inference (CPU); torch is preinstalled in Dockerfile ---
transformers>=4.36.0
huggingface-hub
peft>=0.7.0

# --- SeaweedFS / S3-compatible object store (GGUF adapter delivery) ---
boto3>=1.34.0
botocore>=1.34.0
