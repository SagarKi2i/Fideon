# =============================================================================
# Fideon RunPod Server — Python dependencies
# Python 3.12  |  RunPod GPU environment (CUDA 12.x)
# =============================================================================

# -----------------------------------------------------------------------------
# Web server
# -----------------------------------------------------------------------------
fastapi==0.136.0
uvicorn[standard]==0.45.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# -----------------------------------------------------------------------------
# PDF → image conversion
# -----------------------------------------------------------------------------
PyMuPDF==1.27.2.2
Pillow==12.2.0
numpy==2.4.4

# -----------------------------------------------------------------------------
# Surya OCR — text detection + recognition
# NOTE: 0.6.x uses function-based API (batch_text_detection / batch_recognition)
# DO NOT upgrade to 0.17.x without updating surya_runner.py and extractor.py
# -----------------------------------------------------------------------------
surya-ocr==0.6.13

# -----------------------------------------------------------------------------
# Qwen2-VL — vision-language field extraction
# -----------------------------------------------------------------------------
torch==2.11.0
torchvision==0.26.0
transformers==4.57.6
accelerate==1.13.0
qwen-vl-utils>=0.0.8
