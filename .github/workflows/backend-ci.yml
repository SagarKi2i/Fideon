# ─────────────────────────────────────────────────────────────
# Backend CI/CD
# ─────────────────────────────────────────────────────────────
# Note: this comment is a no-op change to trigger a clean CI rerun.
# Note: no-op change to trigger CI rerun (staging).
# Note: no-op change to trigger CI rerun (demo).

name: Backend CI/CD

on:
  push:
    branches:
      - 'v*-dev'
      - 'v*-staging'
      - 'v1'
    paths:
      - 'backend/**'
      - '.github/workflows/**'

  # Production App Service deploy is manual only: Actions → Backend CI/CD → Run workflow (branch v1, enable deploy).
  workflow_dispatch:
    inputs:
      deploy_to_production:
        description: 'Deploy backend to production App Service (requires branch v1)'
        type: boolean
        default: false

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: false

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    environment: ${{ github.ref_name == 'v1' && 'production' || github.ref_name == 'v1-staging' && 'staging' || 'development' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Docker build installs a large Python stack; reclaim runner disk so pip inside build doesn't hit ENOSPC.
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        working-directory: backend
        run: |
          pip install --upgrade pip
          pip install --retries 5 --timeout 120 -r requirements.txt
          pip install pytest pytest-cov flake8 pgvector

      - name: AI runtime config import smoke test
        working-directory: backend
        run: |
          python - <<'PY'
          from app.core import config
          assert isinstance(config.OPENAI_MODEL, str)
          assert isinstance(config.GROQ_MODEL_CHAT, str)
          assert isinstance(config.RUNPOD_ML_HEALTH_PATH, str)
          print("AI runtime config import smoke test passed")
          PY

      - name: Lint
        working-directory: backend
        continue-on-error: true
        # Rules live in backend/.flake8 — keep CI and local runs aligned.
        run: flake8 . --count --statistics

      - name: Test
        working-directory: backend
        run: |
          if find . -name "test_*.py" -o -name "*_test.py" | grep -q .; then
            # fine_tuning/tests below import fine_tuning.acord125_pipeline, which is not in this tree (ML-Akshay layout mismatch); skip until backend test imports are fixed.
            pytest --cov=. --cov-report=xml \
              --ignore=fine_tuning/tests/test_clean_ocr.py \
              --ignore=fine_tuning/tests/test_ocr_extract.py \
              --ignore=fine_tuning/tests/test_postprocess.py
          else
            echo "No test files found — skipping (non-blocking)"
          fi

      - name: AI dataset pipeline tests
        working-directory: backend
        run: |
          if [ -f "fine_tuning/tests/test_multiform_dataset.py" ]; then
            pytest -q fine_tuning/tests/test_multiform_dataset.py
          else
            echo "AI dataset test file not found — skipping"
          fi

      - name: SonarQube Scan
        if: >
          (github.event_name == 'pull_request' && github.base_ref == 'v1-dev') ||
          (github.event_name == 'push' && github.ref == 'refs/heads/v1-dev')
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          projectBaseDir: backend

      # Quality Gate step intentionally removed for bootstrap:
      # we only need scans to succeed so SonarQube dashboards populate.

      - name: Build Docker Image
        if: >
          github.event_name == 'push' ||
          (github.event_name == 'workflow_dispatch' &&
           github.ref == 'refs/heads/v1' &&
           github.event.inputs.deploy_to_production == 'true')
        run: |
          docker build \
            -t neurapodacr.azurecr.io/backend:${{ github.sha }} \
            backend/

      - name: Login to ACR & Push
        if: |
          (github.event_name == 'push' && (
            github.ref == 'refs/heads/v1-dev' ||
            github.ref == 'refs/heads/v1-staging' ||
            github.ref == 'refs/heads/v1'
          )) ||
          (github.event_name == 'workflow_dispatch' &&
           github.ref == 'refs/heads/v1' &&
           github.event.inputs.deploy_to_production == 'true')
        run: |
          echo "${{ secrets.AZURE_CR_PASSWORD }}" | \
            docker login neurapodacr.azurecr.io \
              -u "${{ secrets.AZURE_CR_USERNAME }}" \
              --password-stdin

          docker push neurapodacr.azurecr.io/backend:${{ github.sha }}
          echo "Pushed neurapodacr.azurecr.io/backend:${{ github.sha }}"

  deploy-dev:
    needs: ci
    if: github.ref == 'refs/heads/v1-dev' && github.event_name == 'push'
    uses: ./.github/workflows/deploy-app-service.yml
    with:
      environment: development
      component: backend
      image_sha: ${{ github.sha }}
    secrets: inherit

  deploy-staging:
    needs: ci
    if: github.ref == 'refs/heads/v1-staging' && github.event_name == 'push'
    uses: ./.github/workflows/deploy-app-service.yml
    with:
      environment: staging
      component: backend
      image_sha: ${{ github.sha }}
    secrets: inherit

  deploy-production:
    needs: ci
    if: >
      github.ref == 'refs/heads/v1' &&
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.deploy_to_production == 'true'
    uses: ./.github/workflows/deploy-app-service.yml
    with:
      environment: production
      component: backend
      image_sha: ${{ github.sha }}
    secrets: inherit
